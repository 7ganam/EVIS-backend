"use strict";

/**
 * application-for-sponsor router
 */

const { createCoreRouter } = require("@strapi/strapi").factories;

module.exports = createCoreRouter(
  "api::application-for-sponsor.application-for-sponsor"
);
