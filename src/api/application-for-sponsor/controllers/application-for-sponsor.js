"use strict";

/**
 * application-for-sponsor controller
 */

const { createCoreController } = require("@strapi/strapi").factories;

module.exports = createCoreController(
  "api::application-for-sponsor.application-for-sponsor"
);
